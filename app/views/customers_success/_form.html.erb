<%= form_with(model: customer_success, local: true) do |form| %>
  <% if customer_success.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_success.errors.count, "error") %> prohibited this customer_success from being saved:</h2>

      <ul>
      <% customer_success.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :nivel %>
    <%= form.number_field :nivel %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.select(:status, CustomerSuccess.statuss.collect { |t| [ "#{t[1]} - #{t[0].humanize}", t[0] ] }, {}, {class: "form-control"}) %> 
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
