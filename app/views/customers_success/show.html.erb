<p id="notice"><%= notice %></p>

<div class="text-end">
	<%= link_to 'Edit', edit_customer_success_path(@customer_success) %> |
	<%= link_to 'Back', customers_success_path %>
</div>

<div class="card">
	<div class="card-header">
		<div class="row">
			<div class="col-11">
				Customer Success Nº <%= @customer_success.id %>
			</div>
			<div class="col-1">
				<div class="text-end">
					<% if @customer_success.status == 'ativo' %>
						<span class="badge text-bg-success">ATIVO</span>
					<% else %>
						<span class="badge text-bg-danger"><%= @customer_success.status.upcase %></span>
					<% end %>
				</div>
			</div>
		</div>
	</div>
	<div class="card-body">
		<h5 class="card-title">
			<strong>Nome:</strong>
			<%= @customer_success.nome %>
		</h5>
		<p class="card-text">
			<strong>Nivel:</strong>
			<%= @customer_success.nivel %>
		</p>
	</div>
</div>
